# API base URL. Leave empty in local dev to use Vite proxy (/api -> localhost:5000).
VITE_API_BASE_URL=

# Default tenant context for standalone development.
VITE_DEFAULT_SUBSCRIBER_ID=1
VITE_DEFAULT_RESTAURANT_ID=
